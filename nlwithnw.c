#include <stdio.h>

int main() {
  /*  int c;
    c = getchar();
    while (c != EOF) {
        if (c == ' ') {
            putchar('\n');
        }
        else {
            putchar(c);
        }
        c = getchar();
    }*/
    /*if you use the whitespace character twice it will count as
    2 new words*/
    
}
